# DOVA — Demo Accounts (Seed)

**UpdatedAt:** July 24, 2026  
**Audience:** Engineering, QA, demos  

Local / seed credentials (in-memory or after `npm run db:seed`):

| Role | Email | Password |
|------|-------|----------|
| Admin | `admin@dova.local` | `admin1234` |
| Supplier | `supplier@dova.local` | `supplier1234` |

Customers: register at `/auth/register`.  
Supplier apply: `/auth/supplier-register`.

~20 sample products in seed/demo data.

**URLs (local):** frontend `http://localhost:3001` · API `http://localhost:3000/api/v1/health`
